# 🔵 Бот модерации для Telegram - Версия 3

## 📋 Что исправлено

### 1. ✅ Глобальный бан теперь работает для всех
**Проблема:** Нельзя было выдать глобальный бан членам команды  
**Решение:** Теперь можно банить членов команды, если у вас есть более высокая роль

```python
# Старая проверка:
if target_role > 0:  # Нельзя банить членов команды
    return "❌ Нельзя забанить члена команды!"

# Новая проверка:
if target_role >= role:  # Проверяем только уровень роли
    return "❌ Нельзя забанить пользователя с такой же или более высокой ролью!"
```

### 2. ✅ Интерактивные инлайн-кнопки для всех команд

**Проблема:** Команды выполнялись сразу без возможности выбора времени/подтверждения  
**Решение:** Добавлены интерактивные кнопки для выбора времени и подтверждения действий

#### Примеры использования:

**Варн с подтверждением:**
```
/warn @user причина
→ Показываются кнопки:
  ✅ Подтвердить варн
  ❌ Отмена
```

**Мут с выбором времени:**
```
/mute @user
→ Показываются кнопки:
  [5 мин] [30 мин] [1 час] [6 часов]
  [1 день] [7 дней] [30 дней] [Навсегда]
  [❌ Отмена]
```

**Бан с выбором времени:**
```
/ban @user
→ Показываются кнопки:
  [5 мин] [30 мин] [1 час] [6 часов]
  [1 день] [7 дней] [30 дней] [Навсегда]
  [❌ Отмена]
```

### 3. ✅ Управление ролями

**Проблема:** Не было возможности назначать и снимать роли  
**Решение:** Добавлены команды `/setrole` и `/removerole`

```bash
# Назначить роль
/setrole @user 5

# Доступные роли:
0: Пользователь
1: Младший модератор
2: Модератор
3: Старший модератор
4: Куратор модерации
5: Технический специалист
6: Главный тех. специалист
7: Куратор групп/каналов
8: Зам. главного модератора
9: Главный модератор
10: Владелец

# Снять роль
/removerole @user
```

**Правила:**
- Нельзя назначить роль выше или равную своей
- Нельзя изменить роль пользователя с ролью выше или равной вашей
- Требуется уровень 7+ для управления ролями

### 4. ✅ Поддержка команд без @botusername

**Проблема:** Команды работали только в формате `/stats@ModerGeorgTgBot @user`  
**Решение:** Теперь работают все варианты:

```bash
# Все эти варианты теперь работают:
/stats @user              # ✅
/stats@BotName @user      # ✅
!ban @user                # ✅
/warn @user               # ✅
```

**Функция `get_args()` улучшена:**
```python
def get_args(message: Message, maxsplit: int = -1) -> list:
    """
    Убирает @botusername из команды автоматически
    Поддерживает префиксы: / ! и другие
    """
    command = parts[0]
    if '@' in command:
        command = command.split('@')[0]  # /stats@bot -> /stats
```

## 🆕 Новые функции

### Интерактивные кнопки на сообщениях
Можно добавить быстрые кнопки модерации к любому сообщению:

```python
builder = create_action_keyboard(target_id, chat_id)
# Создает кнопки:
# [⚠️ Варн] [🔇 Мут]
# [👢 Кик] [🚫 Бан]
# [🧹 Очистить]
```

### Кэш причин варнов
Теперь причины варнов сохраняются для использования в callback:

```python
# При выдаче варна через /warn
await db.cache_warn_reason(target, chat_id, reason)

# При подтверждении через кнопку
reason = await db.get_cached_warn_reason(target, chat_id)

# После выполнения
await db.clear_cached_warn_reason(target, chat_id)
```

## 📊 Новая структура БД

Добавлена таблица `warn_reason_cache`:
```sql
CREATE TABLE warn_reason_cache (
    user_id INTEGER,
    chat_id INTEGER,
    reason TEXT,
    cached_at INTEGER,
    PRIMARY KEY (user_id, chat_id)
);
```

## 🎯 Использование

### Базовые команды

```bash
# Предупреждения
/warn @user причина       # Выдать варн с подтверждением
/unwarn @user            # Снять варн

# Муты
/mute @user              # Мут с выбором времени
/unmute @user            # Размутить

# Баны
/ban @user               # Бан с выбором времени
/unban @user             # Разбанить

# Кик и RO
/kick @user причина      # Кикнуть
/ro @user                # Режим только чтение
/unro @user              # Снять RO

# Глобальные действия
/gban @user причина      # Глобальный бан (уровень 7+)
/ungban @user            # Снять глобальный бан

# Управление ролями
/setrole @user <0-10>    # Назначить роль (уровень 7+)
/removerole @user        # Снять роль (уровень 7+)

# Утилиты
/stats [@user]           # Статистика
/staff                   # Список команды
/setnick @user ник       # Установить ник
/clear <N>               # Очистить N сообщений
```

### Быстрые действия через callback кнопки

```python
# Варн
quickwarn:{target}:{chat_id}

# Мут (с выбором времени)
selectmute:{target}:{chat_id}
applymute:{target}:{chat_id}:{seconds}

# Бан (с выбором времени)
selectban:{target}:{chat_id}
applyban:{target}:{chat_id}:{seconds}

# Кик
quickkick:{target}:{chat_id}

# Очистка
quickclear:{target}:{chat_id}

# Отмена
cancelaction:{target}:{chat_id}
```

## 🔧 Технические детали

### Проверка прав для глобального бана
```python
# Только проверка уровня роли
if target_role >= role:
    return "❌ Нельзя забанить!"
# Больше НЕ проверяем: if target_role > 0
```

### Создание клавиатур
```python
def create_duration_keyboard(action: str, target_id: int, chat_id: int):
    """Клавиатура выбора времени"""
    durations = [
        ("5 мин", 300),
        ("30 мин", 1800),
        ("1 час", 3600),
        ("6 часов", 21600),
        ("1 день", 86400),
        ("7 дней", 604800),
        ("30 дней", 2592000),
        ("Навсегда", 0)
    ]
    # callback_data: action:target:chat:seconds
```

### Парсинг команд
```python
def get_args(message: Message, maxsplit: int = -1) -> list:
    """
    /stats@bot @user -> ['/stats', '@user']
    !ban @user -> ['!ban', '@user']
    /warn @user -> ['/warn', '@user']
    """
```

## 📝 Требования

```txt
aiogram>=3.0.0
aiosqlite>=0.19.0
```

## 🚀 Запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка конфига
nano config.json

# Запуск
python main.py
```

## 📄 Конфигурация

```json
{
  "bot_token": "YOUR_BOT_TOKEN",
  "moderated_chats": [-1001234567890],
  "preset_staff": {
    "username1": 10,
    "username2": 5
  },
  "max_warns": 3,
  "spam_interval_seconds": 2,
  "spam_messages_count": 3,
  "anon_admin_role": 10
}
```

## 🎨 Примеры UI

### Мут с выбором времени
```
🔇 Выберите время мута

Пользователь: @username

Выберите продолжительность:
[5 мин] [30 мин]
[1 час] [6 часов]
[1 день] [7 дней]
[30 дней] [Навсегда]
[❌ Отмена]
```

### Подтверждение варна
```
⚠️ Выдать предупреждение?

Пользователь: @username
Причина: Нарушение правил

Выберите действие:
[✅ Подтвердить варн]
[❌ Отмена]
```

## 🔒 Безопасность

- Все команды проверяют права доступа
- Нельзя модерировать пользователей с равной или более высокой ролью
- Глобальный бан требует уровень 7+
- Управление ролями требует уровень 7+
- Лимиты времени мута зависят от роли модератора

## 📌 Changelog

### v3 (Текущая версия)
- ✅ Исправлен глобальный бан для членов команды
- ✅ Добавлены интерактивные инлайн-кнопки
- ✅ Добавлены команды управления ролями
- ✅ Улучшен парсинг команд (поддержка без @botusername)
- ✅ Добавлен кэш причин варнов
- ✅ Улучшен UI для всех команд модерации

### v2
- Поддержка анонимных админов
- Меню команд через set_my_commands
- Обновление ChatPermissions

### v1
- Базовый функционал модерации

---

**Автор:** Модерация Георгиевка  
**Версия:** 3.0  
**Дата:** 2024
