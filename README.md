# 🔵 Модерация Анонимные сообщения | Георгиевка
## Telegram бот для модерации групп — ИСПРАВЛЕННАЯ ВЕРСИЯ

---

## ✅ Что исправлено

### 1. Конфиг `preset_staff` теперь по user_id
**Было:** username как ключ (бот не мог резолвить пользователей)
**Стало:** user_id как ключ, username опционально для отображения

```json
"preset_staff": {
    "123456789": {"role": 10, "username": "Timo4ka115"},
    "987654321": {"role": 9, "username": "treshshshhh"}
}
```

### 2. Поддержка анонимных сообщений (GroupAnonymousBot)
- Бот корректно определяет ID `1087968824` как анонимного бота
- Не применяет фильтры/антифлуд к анонимным сообщениям
- Команды модерации работают через `@username` или `ID` вместо реплая

### 3. Исправлен поиск участников (`parse_user`)
- Надёжный кэш `username <-> user_id` (двусторонний)
- Резолв через Telegram API с фоллбэком на кэш
- Поддержка: реплай, @username, числовой ID, ник в чате, username без @

### 4. Исправлены `ChatPermissions` (новый Bot API)
- Убраны устаревшие `can_send_media_messages`
- Добавлены все новые поля: `can_send_audios`, `can_send_photos`, `can_send_videos` и т.д.

### 5. Все 11 ролей (0-10) работают корректно
| Роль | Название | Права |
|------|----------|-------|
| 0 | Пользователь | Без прав модерации |
| 1 | Младший модератор | мут (1ч), варн, удаление, кик |
| 2 | Модератор | мут (1ч), варн, удаление, кик |
| 3 | Старший модератор | мут (24ч), бан/разбан, зов, назначение модераторов |
| 4 | Куратор модерации | мут (24ч), снятие варнов |
| 5 | Тех. специалист | мут без лимита, настройки чата, setrole (до 4) |
| 6 | Главный тех. специалист | мут без лимита, настройки чата |
| 7 | Куратор групп/каналов | назначение админов/ст. админов |
| 8 | Зам. главного модератора | назначение админов/ст. админов |
| 9 | Главный модератор | глоб. бан, управление командой, рассылка |
| 10 | Владелец | всё |

### 6. `/staff` через username в Telegram
- В конфиге: по user_id
- В боте: `/addstaff @username 5` — через username
- `/removestaff @username` — через username
- Отображение: показывает @username если известен, иначе ID

### 7. Проверка чатов по chat_id
- `moderated_chats` в конфиге — список ID чатов
- Бот регистрирует чат автоматически при первом сообщении

---

## ⚙️ Настройка config.json

```json
{
    "bot_token": "ВАШ_ТОКЕН_БОТА",

    "moderated_chats": [
        -1003724639162
    ],

    "preset_staff": {
        "USER_ID_1": {"role": 10, "username": "owner_username"},
        "USER_ID_2": {"role": 9, "username": "admin_username"}
    },

    "max_warns": 3,
    "spam_interval_seconds": 2,
    "spam_messages_count": 3
}
```

### Как узнать user_id:
1. Напишите боту @userinfobot в Telegram
2. Или используйте команду /id в группе

### Как узнать chat_id:
1. Добавьте бота в группу
2. Используйте команду /id (без аргументов — покажет ID чата)

---

## 🚀 Запуск

```bash
pip install -r requirements.txt
python main.py
```

---

## 📋 Все команды

### Для всех (роль 0+):
- `/id` — узнать ID пользователя/чата
- `/stats @user` — статистика пользователя
- `/mystatus` — мой статус
- `/staff` — состав команды
- `/top` — топ по сообщениям

### Модераторы (роль 1-2):
- `/mute @user 30m спам` — замутить
- `/unmute @user` — снять мут
- `/warn @user причина` — предупреждение
- `/unwarn @user` — снять варн
- `/kick @user` — кикнуть
- `/del` (реплай) — удалить сообщение
- `/clear @user` — очистить сообщения
- `/setnick @user ник` — установить ник
- `/removenick @user` — удалить ник

### Старший модератор (роль 3-4):
- `/ban @user причина` — забанить
- `/unban @user` — разбанить
- `/zov причина` — вызов всех
- `/addmoder @user` — назначить модератором (роль 1)
- `/removerole @user` — снять роль

### Тех. специалист (роль 5-6):
- `/setrole @user 3` — установить роль
- `/banword слово` — запретить слово
- `/unbanword слово` — разрешить слово
- `/filter` — вкл/выкл фильтр
- `/welcome текст` — приветствие
- `/quiet` — режим тишины
- `/antiflood` — антифлуд

### Куратор (роль 7-8):
- `/addadmin @user` — назначить ст. модератором (роль 3)
- `/addsenadmin @user` — назначить тех. специалистом (роль 5)

### Главный модератор (роль 9-10):
- `/gban @user причина` — глобальный бан
- `/gunban @user` — снять глоб. бан
- `/addstaff @username 5` — добавить в команду
- `/removestaff @username` — удалить из команды
- `/broadcast текст` — рассылка по всем чатам
