# 📝 Полный список команд

## 👤 Команды пользователей (роль 0)

### /help
Показать список доступных команд
```
/help
```

### /stats  
Показать статистику пользователя
```
/stats                    # Ваша статистика
/stats @username          # Статистика по username
/stats (reply)            # Статистика через ответ
```

### /getid
Получить ID пользователя
```
/getid                    # Ваш ID
/getid @username          # ID по username
/getid 123456789          # Проверить ID
/getid (reply)            # ID через ответ
```

---

## 🛡 Команды модераторов (роль >= 1)

### /setnick
Установить ник пользователю
```
/setnick @user Крутой_Ник
/setnick (reply) Новый_Ник
/setnick 123456789 Его_Ник
```

### /removenick (rnick)
Удалить ник
```
/removenick @user
/removenick (reply)
/rnick @user
```

### /getnick
Проверить ник пользователя
```
/getnick @user
/getnick (reply)
```

### /getacc
Найти пользователя по нику
```
/getacc Крутой_Ник
```

### /nlist
Список пользователей с никами (пагинация)
```
/nlist          # Первая страница
/nlist 2        # Вторая страница
```

### /nonick
Пользователи без установленных ников
```
/nonick         # Первая страница
/nonick 2       # Вторая страница
```

### /kick
Исключить пользователя из чата
```
/kick @user                      # Без причины
/kick @user Нарушение правил     # С причиной
/kick (reply) Спам               # Через ответ
```

### /warn
Выдать предупреждение (3 варна = кик)
```
/warn @user Мат в чате
/warn (reply) Оскорбления
```

### /unwarn
Снять одно предупреждение
```
/unwarn @user
/unwarn (reply)
```

### /getwarn
Показать активные предупреждения
```
/getwarn @user
/getwarn (reply)
```

### /warnhistory
История всех предупреждений пользователя
```
/warnhistory @user
/warnhistory (reply)
```

### /warnlist
Список всех пользователей с активными варнами
```
/warnlist
```

### /staff
Показать администрацию беседы
```
/staff
```

### /reg
Дата первого сообщения в чате (регистрация)
```
/reg                # Ваша дата
/reg @user          # Дата пользователя
/reg (reply)
```

### /mute
Замутить пользователя (запретить писать)
```
/mute @user 30 Спам                 # На 30 минут
/mute (reply) 60 Флуд               # На 1 час
/mute 123456789 1440 Нарушение      # На 24 часа
```

### /unmute
Размутить пользователя
```
/unmute @user
/unmute (reply)
```

### /getmute
Информация о муте пользователя
```
/getmute @user
/getmute (reply)
```

### /mutelist
Список всех активных мутов
```
/mutelist
```

### /clear
Очистить все сообщения пользователя в чате
```
/clear @user
/clear (reply)
```

### /getban
Информация о бане пользователя
```
/getban @user
/getban (reply)
```

### /delete
Удалить сообщение (ваше + на которое ответили)
```
/delete (reply на сообщение)
```

---

## ⚔️ Команды старших модераторов (роль >= 2)

### /ban
Забанить пользователя в беседе
```
/ban @user Систематические нарушения
/ban (reply) Мультиаккаунты
```

### /unban
Разбанить пользователя
```
/unban @user
/unban (reply)
```

### /addmoder
Выдать роль модератора (роль 1)
```
/addmoder @user
/addmoder (reply)
```

### /removerole
Забрать роль у пользователя
```
/removerole @user
/removerole (reply)
```

### /banlist
Показать список всех банов в беседе
```
/banlist
```

---

## 👑 Команды администраторов (роль >= 3)

### /quiet
Включить/выключить режим тишины (могут писать только модеры)
```
/quiet          # Переключить
```

### /addsenmoder
Выдать роль старшего модератора (роль 2)
```
/addsenmoder @user
/addsenmoder (reply)
```

### /rnickall
Удалить все ники в беседе
```
/rnickall
```

---

## ⭐️ Команды старших администраторов (роль >= 4)

### /addadmin
Выдать роль администратора (роль 3)
```
/addadmin @user
/addadmin (reply)
```

### /pullinfo
Информация о сетке бесед
```
/pullinfo
```

### /banwords
Управление запрещёнными словами
```
/banwords                    # Показать список
/banwords add мат            # Добавить слово
/banwords remove мат         # Удалить слово
```

### /filter
Включить/выключить фильтр запрещённых слов
```
/filter         # Переключить
```

---

## 🏆 Команды владельца беседы (роль >= 5)

### /antiflood
Включить/выключить защиту от спама
```
/antiflood      # Переключить
```
- При включении: автомут на 5 минут за 3+ сообщения за 2 секунды

### /welcometext
Установить приветственное сообщение
```
/welcometext                              # Показать текущее
/welcometext Привет, %n! Добро пожаловать # Установить новое
/welcometext off                          # Отключить приветствия
```

**Доступные переменные:**
- `%u` - упоминание пользователя
- `%n` - упоминание с именем
- `%i` - ID пригласившего

**Пример:**
```
/welcometext Добро пожаловать, %n! Пригласил: %i
```

### /invite
Включить/выключить режим добавления только модераторами
```
/invite         # Переключить
```
- При включении: обычные пользователи не смогут добавлять людей

### /leave
Включить/выключить исключение при выходе
```
/leave          # Переключить
```
- При включении: пользователь, вышедший сам, будет автоматически исключён

### /addsenadmin
Выдать роль старшего администратора (роль 4)
```
/addsenadmin @user
/addsenadmin (reply)
```

### /setpull
Установить ID сетки бесед
```
/setpull 12345      # Добавить в сетку
/setpull 0          # Убрать из сетки
```

---

## 🔥 Автоматические функции

### Антифлуд
При включённом `/antiflood`:
- 3+ сообщения за 2 секунды = автомут на 5 минут

### Фильтр слов  
При включённом `/filter`:
- Сообщение удаляется
- Мут на 30 минут

### Режим тишины
При включённом `/quiet`:
- Писать могут только модераторы и выше
- Сообщения обычных пользователей удаляются

### Автокик за варны
- 3 предупреждения = автоматическое исключение

---

## 💡 Примеры использования

### Модерация нарушителя
```
/warn @user Первое предупреждение
/warn @user Второе предупреждение  
/warn @user Третье предупреждение  # Автокик
```

### Быстрый мут за спам
```
/mute @user 30 Спам
```

### Установка ролей
```
/addmoder @helper        # Выдать модератора
/addsenmoder @trusted    # Выдать старшего модератора
/addadmin @main_admin    # Выдать администратора
```

### Настройка беседы
```
/antiflood              # Включить антиспам
/filter                 # Включить фильтр
/banwords add мат       # Добавить запрещённое слово
/welcometext Привет, %n! # Установить приветствие
```

### Работа с сеткой
```
/setpull 100            # Добавить в сетку #100
/pullinfo               # Посмотреть информацию
```

---

## ⚠️ Важные заметки

1. **Права доступа**: Нельзя модерировать пользователей с равной или более высокой ролью

2. **Reply-команды**: Большинство команд работают через ответ на сообщение

3. **Username vs ID**: Можно использовать как @username, так и числовой ID

4. **Владелец**: Определяется при первом `/start` (создатель группы)

5. **Роль 6**: Менеджер бота - глобальная роль, выше всех локальных

---

**Версия**: 1.0.0  
**Дата обновления**: 04.02.2026
