version: "3.8"

services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      # Храним SQLite БД на хосте (иначе после рестарта контейнера слетает интерфейс/роли/статистика)
      - ./data:/app/data
      - ./config.json:/app/config.json:ro
