# ⚡ Быстрая установка и обновление v4

## 🎯 Все исправления:

1. ✅ **preset_staff работает по ID** - вместо username теперь используется user_id
2. ✅ **Улучшенная статистика** - больше информации (ник, время мута, причины)
3. ✅ **Глобальный бан требует снятия роли** - для безопасности
4. ✅ **Исправлена команда /help** - теперь работает везде
5. ✅ **Инлайн-кнопки для всех команд** - интерактивный выбор времени/подтверждение

## 📦 Установка

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Настройте config.json:**

⚠️ **ВАЖНО:** Теперь используются ID пользователей, а не username!

```json
{
  "bot_token": "YOUR_BOT_TOKEN",
  "moderated_chats": [-1001234567890],
  "preset_staff": {
    "123456789": 10,
    "987654321": 5
  },
  "max_warns": 3
}
```

**Как узнать свой ID:**
- Напишите боту @userinfobot
- Или используйте команду /stats в любом чате с ботом

3. **Запустите бота:**
```bash
python main.py
```

## 🔄 Обновление с версии 3

### Шаг 1: Обновите config.json

Замените username на user_id:

**Было (v3):**
```json
"preset_staff": {
  "ishakbest": 10,
  "moderator1": 5
}
```

**Стало (v4):**
```json
"preset_staff": {
  "123456789": 10,
  "987654321": 5
}
```

### Шаг 2: Замените main.py

```bash
# Сделайте backup старого файла
cp main.py main_v3_backup.py

# Скопируйте новый файл
cp main.py /path/to/your/bot/
```

### Шаг 3: Перезапустите бота

```bash
# Остановите старую версию
# Ctrl+C или kill процесс

# Запустите новую версию
python main.py
```

**База данных совместима!** Миграция не требуется.

## 🎯 Основные изменения в использовании

### 1. Глобальный бан теперь требует снятия роли

**До версии 4:**
```bash
/gban @moderator
❌ Нельзя забанить члена команды!
```

**В версии 4:**
```bash
/gban @moderator
⚠️ Сначала снимите роль: /removerole @moderator

/removerole @moderator
✅ Роль снята!

/gban @moderator
🌐 [✅ Подтвердить] [❌ Отмена]
```

### 2. Все команды теперь с кнопками

```bash
/mute @user
→ Появляются кнопки выбора времени

/warn @user причина
→ Кнопка подтверждения

/ban @user
→ Выбор времени бана
```

### 3. Улучшенная статистика

```bash
/stats @user
→ Теперь показывает:
  - Ник в чате
  - Время окончания мута
  - Причины мута/бана
  - Информацию о глобальном бане
```

## 🐛 Решение проблем

### Проблема: "Бот не находит админов из preset_staff"
**Решение:** Замените username на user_id в config.json

### Проблема: "Команда /help не работает"
**Решение:** Обновитесь до версии 4

### Проблема: "Не появляются инлайн-кнопки"
**Решение:** Убедитесь что используете main.py из версии 4

### Проблема: "Не могу выдать глобальный бан модератору"
**Решение:** Это не баг! Сначала снимите роль через /removerole

## 📋 Команды для проверки

После установки проверьте:

```bash
# Проверка помощи
/help

# Проверка статистики
/stats

# Проверка команды с кнопками
/mute @user

# Проверка списка команды
/staff
```

## 🆘 Нужна помощь?

1. Проверьте, что используете правильный токен бота
2. Убедитесь что бот добавлен в группу как администратор
3. Проверьте что ID чата правильный в config.json
4. Убедитесь что используете ID пользователей, а не username в preset_staff

---

**Готово!** Теперь у вас работающая версия 4 со всеми исправлениями.
