# 🔄 Обновление v4.1 - Режим RO для всего чата

## ✅ Что исправлено

### Команда /ro теперь работает для ВСЕГО ЧАТА

**Было (v4.0):**
```bash
/ro @user     # Режим RO для конкретного пользователя
/unro @user   # Снять RO с пользователя
```

**Стало (v4.1):**
```bash
/ro           # Включить режим RO для ВСЕГО чата
/unro         # Выключить режим RO для всего чата
```

## 🎯 Как работает

### Включение режима RO
```bash
/ro
```

Результат:
- 👁 **Обычные пользователи (роль 0)** - НЕ могут писать сообщения
- ⭐ **Staff (роль 1+)** - МОГУТ писать сообщения
- 🔇 Сообщения от обычных пользователей автоматически удаляются

### Выключение режима RO
```bash
/unro
```

Результат:
- ✍️ Все пользователи снова могут писать сообщения

## 📋 Примеры использования

### Пример 1: Важное объявление

```bash
# Модератор делает объявление
/ro
👁 Режим только чтение включен!
Обычные пользователи не могут отправлять сообщения.
Staff может продолжать работу.

# Модератор пишет объявление
"⚠️ ВАЖНО: Сегодня в 20:00 будет важное событие..."

# Обычные пользователи не могут ответить
# Только staff может писать

# После объявления
/unro
✍️ Режим только чтение выключен!
Все пользователи могут отправлять сообщения.
```

### Пример 2: Успокоить чат

```bash
# В чате спор/флуд
/ro
👁 Режим RO включен - все успокоились

# Staff обсуждает ситуацию между собой
# Обычные пользователи не могут писать

# Через некоторое время
/unro
✍️ Режим RO выключен - все снова могут писать
```

## 🔧 Технические детали

### Что происходит при включении /ro:

1. В базе данных устанавливается флаг `ro_mode = 1` для чата
2. При каждом сообщении проверяется:
   - Если `role < 1` (обычный пользователь) И `ro_mode = 1`
   - → Сообщение удаляется
3. Staff (роль 1+) может писать без ограничений

### Что происходит при выключении /unro:

1. В базе данных устанавливается флаг `ro_mode = 0` для чата
2. Все пользователи снова могут писать

## 🛡️ Безопасность

- Требуется роль 1+ для использования команд /ro и /unro
- Режим RO НЕ влияет на staff (роль 1+)
- Режим RO работает только в группах, не в ЛС

## ⚙️ Установка обновления

1. **Замените файлы:**
```bash
cp main.py main_v4.0_backup.py
cp new/main.py ./main.py
cp new/db.py ./db.py
```

2. **Перезапустите бота:**
```bash
# Остановите старую версию
# Ctrl+C

# Запустите новую
python main.py
```

**База данных совместима!** Миграция не требуется.

## 📊 Примеры команд

```bash
# Основное использование
/ro        # Включить режим RO
/unro      # Выключить режим RO

# Проверить статус (в логах)
# При включении: "Режим RO включен в чате -1001234567890"
# При выключении: "Режим RO выключен в чате -1001234567890"
```

## ❓ FAQ

**Q: Будут ли удаляться сообщения staff?**
A: Нет, staff (роль 1+) может писать даже в режиме RO

**Q: Показывается ли уведомление при удалении?**
A: Нет, сообщения удаляются тихо чтобы не спамить чат

**Q: Можно ли включить режим RO только для определенных пользователей?**
A: Нет, /ro работает для всех обычных пользователей (роль 0)

**Q: Что если я хочу режим RO для конкретного человека?**
A: Используйте /mute @user на длительный срок

---

**Версия:** 4.1  
**Дата:** Февраль 2026
